services:
  redis:
    image: redis:7.2-alpine
    container_name: redis-container
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    ports:
      - "6379:6379"
